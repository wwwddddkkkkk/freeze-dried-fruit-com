{
  "name": "freeze-dried-fruit-com",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static site for Freeze-Dried-Fruit.com — editorial field guide. Markdown articles + JSON config compile to static HTML for GitHub Pages / Cloudflare Pages.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs && node scripts/serve.mjs",
    "fetch-news": "node scripts/fetch-news.mjs",
    "new-article": "node scripts/new-article.mjs"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2"
  }
}
